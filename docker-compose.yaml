version: '3.4'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb-kbacourses
    ports:
      - 27017:27017
    volumes:
      - mongo_volume:/data/db
  api:
    image: kbacourses-api
    depends_on:
      - mongodb
    build: 
      context: server
      dockerfile: ./dockerfile
    ports:
      - 5000:5002

  ui:
    image: front-end
    depends_on:
      - api
    build:
      context: front-end
      dockerfile: ./dockerfile
    ports:
      - 3000:3002

volumes:
  mongo_volume:
